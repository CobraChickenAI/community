# ── Community ─────────────────────────────────────────────────────────────────

# Set this after running POST /communities — copy the returned 'id'
COMMUNITY_SCOPE_ID=

# Path to the SQLite database (default: community.db in project root)
DB_PATH=community.db

# URL where the registration surface is running (used by agents for verify calls)
SURFACE_URL=http://localhost:8000


# ── Surface ───────────────────────────────────────────────────────────────────

PORT=8000


# ── Discord ───────────────────────────────────────────────────────────────────

# From Discord Developer Portal → Your App → Bot → Token
DISCORD_BOT_TOKEN=

# Comma-separated channel IDs to watch as a Connector (leave empty for all)
# Example: 1234567890,9876543210
DISCORD_WATCH_CHANNELS=


# ── Google Chat (coming) ──────────────────────────────────────────────────────

# GOOGLE_CHAT_SERVICE_ACCOUNT_FILE=
# GOOGLE_CHAT_SPACE_ID=


# ── Notes ─────────────────────────────────────────────────────────────────────
# Bindings (where relays get posted per platform) are registered at runtime via:
#   POST /bindings
# with the scope_slug, platform, and default_channel_id.
# This lets you add/remove surfaces without restarting.
